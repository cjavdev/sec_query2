# encoding: utf-8

include SecQuery

describe SecQuery::Document do
  it "::parse takes in XML and parses data into an instance of SecQuery::Document"
  it "#initialize accepts a hash of attributes to be stored into instance variables"
end
